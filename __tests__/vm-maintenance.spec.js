describe("Vending MAchine", () => {
  describe("Maintenance uses", () => {
    describe("Worker adds change", () => {
      it("Should add provided change to cash count", () => {});
    });
    describe("Worker adds stock", () => {
      it("Should add provided stock to inventory", () => {});
    });
  });
});
